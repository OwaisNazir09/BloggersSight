<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>Home</title>
    <style>
html, body {
    margin: 0;
    padding: 0;
    height: 100%; /* Ensure the body takes the full viewport height */
    display: flex; /* Use flexbox */
    flex-direction: column; /* Align items in a column */
    background-color: rgb(12, 35, 73);
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    background-color: indigo;
    bottom: 0;
    background-image: url("https://img.freepik.com/premium-photo/3d-render-digital-art-fantasy-natural-environment-high-quality-wallpaper_1048944-17359732.jpg?w=1060");
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center center;
   
    z-index: -2; /* Ensure the background stays behind the content */
}



.container {
    flex-grow: 1; /* Ensure it takes up remaining space */
    padding-bottom: 20px; /* Add padding to the bottom to avoid footer overlap */
}

.row {
    margin: 0; /* Remove default margin from row */
}

.card {
    margin: 15px; /* Margin to create space between cards */
    display: flex;
    flex-direction: column;
    border: 2px solid black; /* Add border */
    height: 300px; /* Fixed height */
    width: 100%; /* Take full width of the column */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Drop shadow effect */
    border-radius: 5px; /* Optional: Rounded corners */
    overflow: hidden; /* Prevent content from exceeding card bounds */
}

.card-img-top {
    object-fit: cover;
    height: 150px; /* Adjusted height for the image */
    width: 100%;
    border-radius: 5px 5px 0 0; /* Optional: Rounded corners for the top */
}

.card-body {
    flex-grow: 1;
    overflow-y: auto; /* Allow scrolling if content exceeds the card body */
    max-height: 150px; /* Set max height for card body */
}

.card-title {
    font-size: 1.2rem;
    margin-bottom: 15px;
}

footer {
    background-color: #282c34; /* Add footer styling here */
    color: white;
    padding: 20px;
    text-align: center;
    flex-shrink: 0; /* Prevent footer from shrinking */
}

    </style>
</head>
<body>

    <%- include('./partials/navbar', { currentUser: currentUser }) %>

    <div class="container">
        <div class="row">
            <% allblogs.forEach(blog => { %>
              <div class="col-md-3">
                <div class="card mb-4" style="width: 100%;">
                  <img src="<%= blog.coverImageUrl %>" class="card-img-top" alt="Blog Image">
                  <div class="card-body">
                    <h5 class="card-title"><%= blog.title %></h5>
                    <a href="/blog/<%= blog.id %>" class="btn btn-primary">Read more</a>
                  </div>
                </div>
              </div>
            <% }) %>
        </div>
    </div>
    <%- include('./partials/footer') %>
    <%- include('./partials/script') %>


</body>
</html>
